# Publitz Audits - Simplified Requirements
# Core dependencies for the streamlined system

# Configuration
python-dotenv==1.0.0

# API Clients
anthropic==0.40.0            # Claude AI (required)
requests==2.31.0             # HTTP requests (Steam API, external research)
beautifulsoup4==4.12.3       # Web scraping (HLTB, SteamDB)
lxml==5.1.0                  # HTML parsing

# Report Generation & Export
markdown==3.5.1              # Markdown → HTML conversion
weasyprint==60.2             # HTML → PDF conversion (for Phase 3)
Jinja2==3.1.3                # HTML templates

# Optional: Keep Streamlit if we want a simple UI
# streamlit==1.29.0          # Uncomment if needed
